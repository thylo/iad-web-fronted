---
// Theme Toggle - Démonstration de variables CSS et thèmes
---

<div class="iad-demo" x-data="{ isDark: false }">
    <div class="iad-demo-header">
        <h3>Démo : Thème clair / sombre</h3>
        <button
                class="iad-switch"
                @click="isDark = !isDark"
                :class="{ 'active': isDark }"
                :aria-label="isDark ? 'Passer au thème clair' : 'Passer au thème sombre'"
        >
            <span class="iad-switch-icon" x-text="isDark ? '🌙' : '☀️'"></span>
        </button>
    </div>

    <div class="demo-card" :class="{ 'dark-theme': isDark }">
        <h4>Titre de la carte</h4>
        <p>Ceci est un exemple de texte qui s'adapte automatiquement au thème choisi grâce aux variables CSS.</p>
        <div class="demo-buttons">
            <button class="btn btn-primary">Bouton principal</button>
            <button class="btn btn-secondary">Bouton secondaire</button>
        </div>
    </div>

    <div class="iad-code-output">
        <h4>Variables CSS utilisées</h4>
        <div class="iad-code-block">
            <div>:root &#123;</div>
            <div> --color-background: #FFFFFF;</div>
            <div> --color-text: #333333;</div>
            <div> --color-primary: #FF6B6B;</div>
            <div>&#125;</div>
            <div></div>
            <div>.dark-theme &#123;</div>
            <div> --color-background: #1A1A2E;</div>
            <div> --color-text: #FFFFFF;</div>
            <div> --color-primary: #4ECDC4;</div>
            <div>&#125;</div>
            <div></div>
            <div>/* Les éléments utilisent les variables */</div>
            <div>.card &#123;</div>
            <div> background: var(--color-background);</div>
            <div> color: var(--color-text);</div>
            <div>&#125;</div>
        </div>
    </div>
</div>

<style>
    /* Styles spécifiques à ThemeToggle */
    .demo-card {
        padding: 1.5rem;
        background: #FFFFFF;
        color: #333333;
        border-radius: 8px;
        margin-bottom: 1.5rem;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    .demo-card.dark-theme {
        background: #1A1A2E;
        color: #FFFFFF;
    }

    .demo-card h4 {
        margin: 0 0 1rem 0;
        color: inherit;
    }

    .demo-card p {
        margin: 0 0 1.5rem 0;
        line-height: 1.6;
        color: inherit;
    }

    .demo-buttons {
        display: flex;
        gap: 0.75rem;
        flex-wrap: wrap;
    }

    .demo-card .btn-primary,
    .demo-card .btn-secondary {
        padding: 0.5rem 1rem;
        border-radius: 6px;
        font-size: 0.9rem;
        cursor: pointer;
        transition: all 0.2s;
    }

    .demo-card .btn-primary {
        border: none;
        background: #FF6B6B;
        color: white;
    }

    .demo-card.dark-theme .btn-primary {
        background: #4ECDC4;
        color: #1A1A2E;
    }

    .demo-card .btn-primary:hover {
        opacity: 0.9;
        transform: translateY(-1px);
    }

    .demo-card .btn-secondary {
        background: transparent;
        border: 1px solid #FF6B6B;
        color: #FF6B6B;
    }

    .demo-card.dark-theme .btn-secondary {
        color: #4ECDC4;
        border-color: #4ECDC4;
    }

    .demo-card .btn-secondary:hover {
        background: rgba(255, 107, 107, 0.1);
    }

    .demo-card.dark-theme .btn-secondary:hover {
        background: rgba(78, 205, 196, 0.1);
    }
    .btn{
        margin-top: 0;
    }
</style>
