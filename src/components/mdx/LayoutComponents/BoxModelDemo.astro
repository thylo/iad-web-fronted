  ---
// Composant de démonstration du Box Model
// Permet de visualiser la différence entre content-box et border-box
---

<div class="box-model-demo" x-data="{
  boxSizing: 'border-box',
  width: 300,
  padding: 20,
  border: 5,
  margin: 10,
  get totalWidth() {
    if (this.boxSizing === 'border-box') {
      return this.width;
    }
    return this.width + (this.padding * 2) + (this.border * 2);
  },
  get contentWidth() {
    if (this.boxSizing === 'border-box') {
      return this.width - (this.padding * 2) - (this.border * 2);
    }
    return this.width;
  }
}">
  <div class="controls">
    <div class="control-group">
      <label>
        <strong>box-sizing :</strong>
      </label>
      <div class="radio-group">
        <label>
          <input type="radio" x-model="boxSizing" value="content-box">
          content-box (défaut)
        </label>
        <label>
          <input type="radio" x-model="boxSizing" value="border-box">
          border-box (recommandé)
        </label>
      </div>
    </div>

    <div class="slider-controls">
      <div class="slider-group">
        <label>
          width: <strong x-text="width + 'px'"></strong>
        </label>
        <input type="range" x-model="width" min="100" max="400" step="10">
      </div>

      <div class="slider-group">
        <label>
          padding: <strong x-text="padding + 'px'"></strong>
        </label>
        <input type="range" x-model="padding" min="0" max="50" step="5">
      </div>

      <div class="slider-group">
        <label>
          border: <strong x-text="border + 'px'"></strong>
        </label>
        <input type="range" x-model="border" min="0" max="20" step="1">
      </div>

      <div class="slider-group">
        <label>
          margin: <strong x-text="margin + 'px'"></strong>
        </label>
        <input type="range" x-model="margin" min="0" max="40" step="5">
      </div>
    </div>
  </div>

  <div class="visualization">
    <div class="box-wrapper" :style="`padding: ${margin}px`">
      <div class="box-margin" :style="`padding: ${margin}px; background: rgba(255, 193, 7, 0.2);`">
        <div class="box-border" :style="`border: ${border}px solid #E74C3C; padding: ${padding}px; width: ${width}px; box-sizing: ${boxSizing}; background: rgba(78, 205, 196, 0.3);`">
          <div class="box-content" style="background: rgba(93, 95, 239, 0.3); padding: 1rem; text-align: center; font-weight: 600;">
            Contenu
          </div>
        </div>
      </div>
    </div>

    <div class="measurements">
      <div class="measurement-item">
        <span class="label">Largeur du contenu :</span>
        <strong x-text="contentWidth + 'px'"></strong>
      </div>
      <div class="measurement-item">
        <span class="label">Largeur totale (avec margin) :</span>
        <strong x-text="(totalWidth + margin * 2) + 'px'"></strong>
      </div>
      <div class="measurement-item" x-show="boxSizing === 'content-box'" style="color: #E74C3C;">
        ⚠️ Attention : la largeur totale dépasse la valeur de width !
      </div>
      <div class="measurement-item" x-show="boxSizing === 'border-box'" style="color: #2ECC71;">
        ✅ La largeur de l'élément = width (plus prévisible)
      </div>
    </div>
  </div>

  <div class="code-output">
    <p><strong>Code CSS généré :</strong></p>
    <div class="code-block">
      <div>.box &#123;</div>
      <div>  box-sizing: <span x-text="boxSizing"></span>;</div>
      <div>  width: <span x-text="width + 'px'"></span>;</div>
      <div>  padding: <span x-text="padding + 'px'"></span>;</div>
      <div>  border: <span x-text="border + 'px'"></span> solid #E74C3C;</div>
      <div>  margin: <span x-text="margin + 'px'"></span>;</div>
      <div>&#125;</div>
    </div>
  </div>
</div>

<style>
  .box-model-demo {
    background: #f8f9fa;
    border: 2px solid #e1e8ed;
    border-radius: 0.75rem;
    padding: 1.5rem;
    margin: 2rem 0;
  }

  .controls {
    margin-bottom: 2rem;
  }

  .control-group {
    margin-bottom: 1.5rem;
  }

  .control-group label {
    display: block;
    margin-bottom: 0.5rem;
  }

  .radio-group {
    display: flex;
    gap: 1.5rem;
    flex-wrap: wrap;
  }

  .radio-group label {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    cursor: pointer;
  }

  .radio-group input[type="radio"] {
    cursor: pointer;
  }

  .slider-controls {
    display: grid;
    gap: 1rem;
  }

  .slider-group {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .slider-group label {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 0.95rem;
  }

  .slider-group input[type="range"] {
    width: 100%;
    cursor: pointer;
  }

  .visualization {
    margin: 2rem 0;
    padding: 2rem;
    background: white;
    border-radius: 0.5rem;
    overflow: auto;
  }

  .box-wrapper {
    display: inline-block;
    background: rgba(255, 193, 7, 0.1);
    border: 2px dashed rgba(255, 193, 7, 0.5);
  }

  .box-margin {
    border-radius: 0.25rem;
  }

  .box-border {
    background-clip: padding-box;
  }

  .box-content {
    border-radius: 0.25rem;
    min-height: 60px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .measurements {
    margin-top: 2rem;
    padding: 1rem;
    background: #f8f9fa;
    border-radius: 0.5rem;
  }

  .measurement-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.5rem 0;
    border-bottom: 1px solid #e1e8ed;
  }

  .measurement-item:last-child {
    border-bottom: none;
  }

  .measurement-item .label {
    color: #666;
  }

  .code-output {
    margin-top: 1.5rem;
    padding: 1rem;
    background: #1e1e1e;
    color: #d4d4d4;
    border-radius: 0.5rem;
    font-family: 'Monaco', 'Menlo', monospace;
    font-size: 0.9rem;
  }

  .code-output pre {
    margin: 0.5rem 0 0 0;
  }

  .code-output code {
    color: #d4d4d4;
  }

  .code-output span {
    color: #4EC9B0;
  }

  .code-output strong {
    color: #ffc107;
  }
</style>
