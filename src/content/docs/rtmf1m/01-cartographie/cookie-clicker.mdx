---
title: Cookie Clicker - Apprendre Svelte par la pratique
description: Découverte d'œuvres marquantes et apprentissage des fondamentaux Svelte en construisant un cookie clicker
---

## Vue d'ensemble

**Objectif :** Explorer des œuvres inspirantes et maîtriser les fondamentaux Svelte en construisant un cookie clicker

**Format :** Avec 2-3 étudiants, cette séance est un atelier pratique où vous codez ensemble.

---

## Partie 1 : Exploration d'œuvres marquantes

### Format

Contrairement à la séance initialement prévue, les étudiants n'ont pas exploré d'œuvres. On va donc le faire **ensemble**.

**Approche :** 5-6 exemples concrets, testés en direct, discussion ouverte.

### Exemples à montrer

Pour chaque exemple :
1. **Tester ensemble**
2. **Discussion** : Qu'est-ce qui fonctionne ? Quel territoire ?

#### 1. Patatap - Interface expérimentale

**Lien :** [patatap.com](https://patatap.com/)

**Test :** Tapez sur le clavier, testez différentes touches.

**Discussion :**
- Geste simple → effet audiovisuel complexe
- Pas de tutoriel, découverte par expérimentation
- Territoire : **Interfaces expérimentales** + **Outils créatifs**

---

#### 2. neal.fun - Petites expériences ludiques

**Lien :** [neal.fun](https://neal.fun/)

**Exemples à tester :**
- **The Deep Sea** : scroll infini dans l'océan
- **The Size of Space** : échelle de l'univers
- **Infinite Craft** : combinaison d'éléments

**Discussion :**
- Simplicité de l'interaction
- Scroll ou clic comme seul geste
- Territoire : **Interfaces expérimentales** + **Lecture augmentée**

---

#### 3. Incredibox - Outil créatif musical

**Lien :** [incredibox.com](https://www.incredibox.com/)

**Test :** Drag and drop de personnages pour créer de la musique.

**Discussion :**
- Interface tactile intuitive
- Création sans compétence musicale
- Territoire : **Outils créatifs**

---

#### 4. The Pudding - Data storytelling

**Lien :** [pudding.cool](https://pudding.cool/)

**Exemples à voir :**
- **The Office** dialogue analysis
- **Hip Hop vocabulary**

**Discussion :**
- Données rendues narratives
- Scroll + interactivité
- Territoire : **Lecture augmentée**

---

#### 5. Small Bang Cinemacity - Expérience muséale

**Lien :** [small-bang.com/cinemacity](https://www.small-bang.com/cinemacity/)

**Montrer :** Balade cinéma dans Paris via QR codes.

**Discussion :**
- Lien physique/numérique
- QR codes narratifs
- Territoire : **Expériences muséales**

---

#### 6. Chronicles of Crime - Jeu de société hybride

**Montrer :** Vidéo ou screenshots du jeu.

**Discussion :**
- Application compagnon pour jeu de société
- Numérique assiste mais ne domine pas
- Territoire : **Jeux de société hybrides**

---

### Synthèse collective

**Questions :**
- Quel exemple vous a le plus marqué ?
- Quel territoire vous attire spontanément ?
- Premières idées qui émergent ?

**Transition :**
> "Vous avez vu ce que le web permet. Maintenant, on va apprendre à construire des interactions. On va créer un cookie clicker pour comprendre Svelte."

---

---

## Partie 2 : Cookie Clicker - Niveau 1

### Objectif

Créer un cookie clicker basique pour comprendre :
- Réactivité automatique
- Événements
- Conditions d'affichage

### Étape 1 : Setup du projet

**Vérifier que tous les étudiants ont :**
- Terminal ouvert
- Serveur Vite qui tourne (`npm run dev`)
- Projet dans VSCode/PhpStorm

**Si besoin de créer un nouveau composant :**

**macOS/Linux :**
```bash
mkdir -p src/lib
touch src/lib/CookieClicker.svelte
```

**Windows PowerShell :**
```powershell
mkdir src\lib -Force
New-Item src\lib\CookieClicker.svelte
```

**Alternative :** Créer le fichier directement dans l'éditeur.

---

### Étape 2 : Cookie Clicker basique

**Créer `src/lib/CookieClicker.svelte` :**

```svelte
<script>
  // État réactif : nombre de cookies (Svelte 5 rune)
  let cookies = $state(0);
  
  // Fonction pour cliquer sur le cookie
  function cliquerCookie() {
    cookies += 1; // Incrémentation simple
  }
</script>

<!-- Interface -->
<div class="container">
  <h1>Cookie Clicker</h1>
  
  <!-- Affichage du score -->
  <p class="score">🍪 {cookies} cookies</p>
  
  <!-- Le cookie cliquable -->
  <button class="cookie" on:click={cliquerCookie}>
    🍪
  </button>
  
  <!-- Messages conditionnels selon le score -->
  {#if cookies < 10}
    <p class="message">Cliquez sur le cookie !</p>
  {:else if cookies < 50}
    <p class="message">Bien ! Continuez !</p>
  {:else if cookies < 100}
    <p class="message">Impressionnant !</p>
  {:else}
    <p class="message">🎉 Vous êtes un maître du cookie !</p>
  {/if}
</div>

<style>
  .container {
    max-width: 600px;
    margin: 0 auto;
    padding: 2rem;
    text-align: center;
  }
  
  h1 {
    color: #333;
    margin-bottom: 1rem;
  }
  
  .score {
    font-size: 2rem;
    font-weight: bold;
    color: #e67e22;
    margin: 1rem 0;
  }
  
  .cookie {
    font-size: 8rem;
    background: none;
    border: none;
    cursor: pointer;
    transition: transform 0.1s ease;
    padding: 0;
    margin: 2rem 0;
  }
  
  .cookie:hover {
    transform: scale(1.1);
  }
  
  .cookie:active {
    transform: scale(0.95);
  }
  
  .message {
    font-size: 1.2rem;
    color: #666;
    font-style: italic;
  }
</style>
```

**Utiliser le composant dans `src/App.svelte` :**

```svelte
<script>
  import CookieClicker from './lib/CookieClicker.svelte';
</script>

<main>
  <CookieClicker />
</main>

<style>
  main {
    padding: 2rem;
  }
</style>
```

---

### Étape 3 : Explication des concepts

**1. Réactivité automatique avec `$state()`**

```javascript
let cookies = $state(0); // Variable réactive avec la rune $state()

function cliquerCookie() {
  cookies += 1; // Simple assignation → interface mise à jour automatiquement
}
```

**La rune `$state()`** rend la variable réactive. Pas besoin de `setState`, `this.$set`, ou autre. Une simple assignation suffit.

---

**2. Événements**

```svelte
<button on:click={cliquerCookie}>
```

**Syntax :** `on:nomEvenement={fonction}`

**Autres événements utiles :**
- `on:click` : clic
- `on:dblclick` : double-clic
- `on:mouseover` : survol
- `on:input` : saisie de texte
- `on:keydown` : touche clavier

---

**3. Conditions avec `{#if}` (inchangé)**

```svelte
{#if cookies < 10}
  <p>Message pour débutant</p>
{:else if cookies < 50}
  <p>Message intermédiaire</p>
{:else}
  <p>Message expert</p>
{/if}
```

**Syntax :**
- `{#if condition}` : ouvre le bloc conditionnel
- `{:else if condition}` : condition alternative
- `{:else}` : sinon
- `{/if}` : ferme le bloc

---

---

## Partie 3 : Cookie Clicker - Niveau 2

### Objectif

Ajouter de la complexité pour comprendre :
- Props (passer des données à un composant)
- Listes avec `{#each}`
- État complexe (cookies par seconde)

### Étape 1 : Créer le composant Upgrade

**Créer `src/lib/Upgrade.svelte` :**

```svelte
<script>
  // Props : données passées par le composant parent (Svelte 5 rune)
  let { 
    nom,              // Nom de l'upgrade
    prix,             // Prix en cookies
    gain,             // Cookies par seconde gagnés
    quantite,         // Nombre possédé
    cookiesActuels,   // Pour savoir si on peut acheter
    onacheter         // Callback au lieu d'événement custom
  } = $props();
  
  // Réactivité dérivée avec $derived() (Svelte 5)
  let peutAcheter = $derived(cookiesActuels >= prix);
</script>

<div class="upgrade" class:disabled={!peutAcheter}>
  <div class="info">
    <h3>{nom}</h3>
    <p class="description">+{gain} 🍪/sec</p>
    <p class="quantite">Possédé : {quantite}</p>
  </div>
  
  <button 
    class="acheter" 
    on:click={onacheter}
    disabled={!peutAcheter}
  >
    {prix} 🍪
  </button>
</div>

<style>
  .upgrade {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem;
    margin: 0.5rem 0;
    background: #f8f9fa;
    border-radius: 8px;
    border: 2px solid #dee2e6;
    transition: all 0.2s ease;
  }
  
  .upgrade:hover {
    background: #e9ecef;
  }
  
  .upgrade.disabled {
    opacity: 0.5;
  }
  
  .info {
    text-align: left;
  }
  
  h3 {
    margin: 0 0 0.25rem 0;
    color: #333;
    font-size: 1.1rem;
  }
  
  .description {
    margin: 0.25rem 0;
    color: #666;
    font-size: 0.9rem;
  }
  
  .quantite {
    margin: 0.25rem 0;
    color: #999;
    font-size: 0.85rem;
  }
  
  .acheter {
    padding: 0.5rem 1rem;
    background: #e67e22;
    color: white;
    border: none;
    border-radius: 6px;
    font-weight: bold;
    cursor: pointer;
    font-size: 1rem;
  }
  
  .acheter:hover:not(:disabled) {
    background: #d35400;
  }
  
  .acheter:disabled {
    cursor: not-allowed;
    opacity: 0.5;
  }
</style>
```

---

### Étape 2 : Mettre à jour CookieClicker

**Modifier `src/lib/CookieClicker.svelte` :**

```svelte
<script>
  import { onMount } from 'svelte';
  import Upgrade from './Upgrade.svelte';
  
  // État du jeu (Svelte 5 runes)
  let cookies = $state(0);
  let cookiesParSeconde = $state(0);
  
  // Liste des upgrades disponibles
  let upgrades = $state([
    { id: 1, nom: 'Curseur', prix: 10, gain: 1, quantite: 0 },
    { id: 2, nom: 'Grand-mère', prix: 50, gain: 5, quantite: 0 },
    { id: 3, nom: 'Ferme à cookies', prix: 200, gain: 20, quantite: 0 },
    { id: 4, nom: 'Usine', prix: 500, gain: 50, quantite: 0 },
  ]);
  
  // Fonction pour cliquer sur le cookie
  function cliquerCookie() {
    cookies += 1;
  }
  
  // Fonction pour acheter un upgrade
  function acheterUpgrade(id) {
    // Trouver l'upgrade dans la liste
    const upgrade = upgrades.find(u => u.id === id);
    
    // Vérifier si on a assez de cookies
    if (cookies >= upgrade.prix) {
      cookies -= upgrade.prix; // Déduire le prix
      upgrade.quantite += 1; // Incrémenter la quantité
      upgrade.prix = Math.floor(upgrade.prix * 1.5); // Augmenter le prix pour le prochain achat
      
      // Recalculer les cookies par seconde
      calculerCookiesParSeconde();
    }
  }
  
  // Calculer le total de cookies par seconde
  function calculerCookiesParSeconde() {
    cookiesParSeconde = upgrades.reduce((total, upgrade) => {
      return total + (upgrade.gain * upgrade.quantite);
    }, 0);
  }
  
  // Au montage du composant : démarrer le timer
  // En Svelte 5, onMount peut retourner une fonction de cleanup
  onMount(() => {
    const interval = setInterval(() => {
      cookies += cookiesParSeconde;
    }, 1000); // Chaque seconde
    
    // Fonction de cleanup automatique
    return () => clearInterval(interval);
  });
</script>

<div class="container">
  <div class="game">
    <!-- Colonne gauche : le cookie -->
    <div class="cookie-zone">
      <h1>Cookie Clicker</h1>
      
      <p class="score">🍪 {Math.floor(cookies)} cookies</p>
      <p class="rate">+{cookiesParSeconde} cookies/sec</p>
      
      <button class="cookie" on:click={cliquerCookie}>
        🍪
      </button>
      
      {#if cookies < 10}
        <p class="message">Cliquez sur le cookie !</p>
      {:else if cookiesParSeconde === 0}
        <p class="message">Achetez votre premier upgrade →</p>
      {:else}
        <p class="message">🎉 Votre empire grandit !</p>
      {/if}
    </div>
    
    <!-- Colonne droite : les upgrades -->
    <div class="upgrades-zone">
      <h2>Upgrades</h2>
      
      {#each upgrades as upgrade (upgrade.id)}
        <Upgrade 
          nom={upgrade.nom}
          prix={upgrade.prix}
          gain={upgrade.gain}
          quantite={upgrade.quantite}
          cookiesActuels={cookies}
          onacheter={() => acheterUpgrade(upgrade.id)}
        />
      {/each}
    </div>
  </div>
</div>

<style>
  .container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 2rem;
  }
  
  .game {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
  }
  
  .cookie-zone {
    text-align: center;
  }
  
  h1 {
    color: #333;
    margin-bottom: 1rem;
  }
  
  .score {
    font-size: 2rem;
    font-weight: bold;
    color: #e67e22;
    margin: 1rem 0 0.5rem 0;
  }
  
  .rate {
    font-size: 1.2rem;
    color: #27ae60;
    margin: 0 0 1rem 0;
  }
  
  .cookie {
    font-size: 8rem;
    background: none;
    border: none;
    cursor: pointer;
    transition: transform 0.1s ease;
    padding: 0;
    margin: 2rem 0;
  }
  
  .cookie:hover {
    transform: scale(1.1);
  }
  
  .cookie:active {
    transform: scale(0.95);
  }
  
  .message {
    font-size: 1.2rem;
    color: #666;
    font-style: italic;
  }
  
  .upgrades-zone {
    background: #fff;
    padding: 1rem;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  }
  
  h2 {
    margin-top: 0;
    color: #333;
  }
  
  @media (max-width: 768px) {
    .game {
      grid-template-columns: 1fr;
    }
  }
</style>
```

---

### Étape 3 : Explication des nouveaux concepts

**1. Props : passer des données à un composant enfant (Svelte 5)**

**Dans le composant enfant (Upgrade.svelte) :**
```javascript
let { nom, prix } = $props();
```

**La rune `$props()`** récupère les props passées par le parent. Plus besoin de `export let`.

**Dans le composant parent (CookieClicker.svelte) :**
```svelte
<Upgrade 
  nom={upgrade.nom}
  prix={upgrade.prix}
/>
```

Le passage des props reste identique.

---

**2. Listes avec `{#each}`**

```svelte
{#each upgrades as upgrade (upgrade.id)}
  <Upgrade ... />
{/each}
```

**Syntax :**
- `{#each liste as element (clé unique)}` : boucle
- `(upgrade.id)` : clé unique pour optimiser le rendu
- `{/each}` : ferme la boucle

---

**3. Événements custom → Callbacks (Svelte 5)**

**En Svelte 5, les événements custom sont remplacés par des props callback.**

**Dans le composant enfant (Upgrade.svelte) :**
```javascript
let { onacheter } = $props();

// Appeler directement la fonction
<button on:click={onacheter}>
```

**Dans le composant parent (CookieClicker.svelte) :**
```svelte
<Upgrade onacheter={() => acheterUpgrade(upgrade.id)} />
```

Notez le changement : `on:acheter` devient `onacheter` (une prop fonction).

---

**4. Réactivité dérivée avec `$derived()` (Svelte 5)**

```javascript
let peutAcheter = $derived(cookiesActuels >= prix);
```

**La rune `$derived()`** recalcule automatiquement la valeur quand ses dépendances changent. C'est le remplacement de `$:` pour les valeurs dérivées.

---

**5. Lifecycle avec `onMount` (Svelte 5 simplifié)**

```javascript
import { onMount } from 'svelte';

onMount(() => {
  // Code exécuté quand le composant est créé
  const interval = setInterval(() => {
    cookies += cookiesParSeconde;
  }, 1000);
  
  // En Svelte 5, retourner une fonction la rend automatiquement fonction de cleanup
  return () => clearInterval(interval);
});
```

**Svelte 5 simplifie** : plus besoin de `onDestroy`, juste retourner la fonction de nettoyage depuis `onMount`.

---

## Partie 4 : Mission et prochaines étapes

### Synthèse de ce qu'on a appris

**Concepts Svelte 5 maîtrisés :**
- ✅ Réactivité avec `$state()`
- ✅ Événements DOM (`on:click`)
- ✅ Callbacks (remplacement des événements custom)
- ✅ Conditions (`{#if}`)
- ✅ Listes (`{#each}`)
- ✅ Props avec `$props()` (communication parent → enfant)
- ✅ Callbacks (communication enfant → parent)
- ✅ Lifecycle (`onMount` avec cleanup)
- ✅ Réactivité dérivée avec `$derived()`

**Ce qu'on peut construire avec ça :**
- Interfaces interactives
- Jeux simples
- Expériences narratives
- Outils créatifs

---

### Recap des 5 territoires

**[→ Voir la page complète des territoires](/rtmf1m/territoires/)**

1. **[Expériences muséales/spatiales](/rtmf1m/territoires/01-museales)**
2. **[Jeux de société hybrides](/rtmf1m/territoires/02-jeux-societe)**
3. **[Expériences de lecture augmentées](/rtmf1m/territoires/03-lecture)**
4. **[Interfaces expérimentales/ludiques](/rtmf1m/territoires/04-interfaces)**
5. **[Outils créatifs et générateurs](/rtmf1m/territoires/05-outils-creatifs)**

---

### Mission pour la prochaine séance

#### 1. Explorer des œuvres (obligatoire)

**Trouvez 3 à 5 œuvres** qui vous inspirent dans votre territoire préféré.

**Pour chaque œuvre, notez dans votre carnet :**
- Nom et lien
- Description de l'expérience
- Ce qui fonctionne / ne fonctionne pas
- Pourquoi ça vous inspire

**Où chercher :**
- [Galerie complète](/ressources/galerie/)
- [Awwwards](https://www.awwwards.com/)
- [neal.fun](https://neal.fun/)
- [The Pudding](https://pudding.cool/)
- [Incredibox](https://www.incredibox.com/)

#### 2. Enrichir votre cookie clicker (optionnel mais recommandé)

**Idées d'améliorations :**

**Facile :**
- Ajouter plus d'upgrades
- Changer les emojis et le thème visuel
- Ajouter des sons au clic
- Sauvegarder le score dans LocalStorage

**Moyen :**
- Ajouter des achievements (badges)
- Créer des combos de clics
- Animations de particules au clic
- Thème jour/nuit

**Avancé :**
- Mini-jeux bonus
- Système de prestige (reset avec bonus)
- Graphique de progression
- Mode multiplayer (partage de score)

#### 3. Commencer à réfléchir à votre projet

**Répondez à ces 3 questions :**

**Intention :** Que voulez-vous faire ressentir/découvrir ?  
**Geste :** Quelle action fait l'utilisateur ?  
**Synergie :** Comment le geste et l'intention se renforcent ?

**Exemple :**
- **Intention :** Faire ressentir la solitude d'un personnage
- **Geste :** Cliquer pour interagir, mais le personnage s'éloigne
- **Synergie :** Plus on essaie de se rapprocher, plus il fuit → frustration = émotion

---

### Ressources

**Documentation Svelte :**
- [Tutorial interactif](https://svelte.dev/tutorial)
- [Documentation API](https://svelte.dev/docs)
- [REPL (tester en ligne)](https://svelte.dev/repl)

**Aide technique :**
- [Guide terminal Windows/macOS](/rtmf1m/ressources/terminal-guide)
- [Utilisation de l'IA](/rtmf1m/ressources/utilisation-ia)

**Si bloqué :**
- Demandez à l'enseignant
- Utilisez ChatGPT/Claude (mais comprenez le code généré)
- Consultez la documentation Svelte

---

## Checklist de fin de séance

- [ ] Vous avez exploré 5-6 œuvres web marquantes
- [ ] Vous avez identifié un territoire qui vous attire
- [ ] Votre cookie clicker fonctionne (niveau 1 ou 2)
- [ ] Vous comprenez les concepts Svelte vus aujourd'hui
- [ ] Vous savez où chercher des œuvres inspirantes
- [ ] Vous avez la mission pour la prochaine séance

---

## Prochaines séances

**Module 2 : Fondamentaux techniques**
- Interactions avancées (drag, gestes, clavier)
- Animations et transitions
- Sons et effets visuels
- Recettes de code

**Module 3 : Idéation et prototypage**
- Workshop d'idéation
- Définition du projet personnel
- Prototypage et validation technique

---

## Ce qu'on a accompli ensemble

- ✅ Exploration d'œuvres inspirantes dans les 5 territoires
- ✅ Cookie clicker fonctionnel avec réactivité Svelte
- ✅ Compréhension des props et événements
- ✅ Gestion de listes avec `{#each}`
- ✅ État complexe avec cookies par seconde
- ✅ Premières idées de territoire de projet

**La prochaine étape ?** Approfondir les interactions et commencer à concevoir votre projet personnel.
